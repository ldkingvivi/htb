
gobuster

1. -f add "/"
2. -x php, txt 






====================
tty

python -c "import pty;pty.spawn('/bin/bash')"
or
/usr/bin/script -qc /bin/bash /dev/null

stty raw -echo
fg



=====================
try to get root

sudo -l

ps -ef 
ps -aux

find / -perm -4000 2>/dev/null

something take a input value that we can submit with ; ls -a or "; bin we create"

something calling something that doesn't have a a absolute path, so we can re-create that file and make sure it's first in our PATH




